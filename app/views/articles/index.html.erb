<div>
  <h2 >Wikinimous</h2>
  <div class="container">
    <ul class="ul">
      <% @articles.each do |article|%>
        <li><%= link_to article.title, article_path(article), class: "title"%></li>
        <li><%= article.content%></li>
      <%end%>
    </ul>
  <%= link_to "New article", new_article_path%>
  </div>
</div>
